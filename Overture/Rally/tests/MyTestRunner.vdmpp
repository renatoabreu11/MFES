class MyTestRunner

operations
	public static run() == (
		IO`println("Initializing Test Runner");
		
		new ChampionshipTest().Run();
    new PartnerTest().Run();
    new SponsorTest().Run();    
    new ManufacturerTest().Run();
    new CarTest().Run();
    
    IO`println("Finalizing Test Runner");
	);
	
	private SystemTest: () ==> ()
	 SystemTest() == (
		 IO`println("\nInitializing System Test");
	   
	   IO`println("\nFinalizing System Test");
	 )
	
end MyTestRunner