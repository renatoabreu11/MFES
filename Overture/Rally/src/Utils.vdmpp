class Utils

types
	public String = seq of char;
	public String1 = seq1 of char;
	public Country = <Italy> | <France> | <Spain> | <SouthKorea> |
       <Germany> | <GreatBritain> | <Japan>;

functions
	public static SubString(s: String, offset, length: nat) res:String ==
		s((offset + 1), ... ,  (offset + length))
		pre length = 0 or offset + length <= len s;
    
end Utils