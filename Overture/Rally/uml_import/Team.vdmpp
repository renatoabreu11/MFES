class Team
instance variables
  drivers:set of (Driver);
  sponsors:set of (Sponsor);
  backer:[Manufacturer];
  car:set of (Car);
  name:seq of (char);
  based:Country;
  established:seq of (char);

end Team
